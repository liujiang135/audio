<div class="container">
    <div class="text-center flex-column logo-img-top">
        <div class="text-center">
            <image src="{{ $t('files.cpLogo') }}" class="logo-img">
            </image>
        </div>
        <text class="{{ isNetValue ? 'net-name' : 'fa-name' }} font-65ST">{{ faNameValue }}
        </text>
        <text class="fa-version font-55ST" if="{{ actionType == 'showVersion' }}">{{ versionValue }}
        </text>
    </div>
</div>